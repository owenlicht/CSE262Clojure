(in-ns 'clojureIntro.core)

; --------------------------------
; Question 11
; --------------------------------
; You must do this queastion with map
; fn main() {
;   let x = vec![1, 2, 3];
;   let y: Vec<i32> = x.iter().map(|x| x * 2).collect();
;   let question_11 = y[1];
; }


(def question-11 0)
